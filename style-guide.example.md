# 文档风格指南示例

这是一个示例风格指南文件，用于自定义文档生成风格。
在 `config.yaml` 中配置 `customization.style_guide` 路径即可生效。

---

## API 文档格式

### 参数说明
- 使用表格形式展示参数
- 每个参数必须包含：参数名、类型、默认值、是否必填、说明

示例：

| 参数名 | 类型 | 默认值 | 必填 | 说明 |
|--------|------|--------|------|------|
| `name` | `str` | 无 | 是 | 用户名称 |
| `timeout` | `int` | `30` | 否 | 超时时间（秒） |

### 返回值说明
- 必须包含返回类型
- 提供返回值示例

示例：
```python
Returns:
    dict: 包含用户信息的字典
    {
        "id": 1,
        "name": "张三"
    }
```

### 异常说明
- 列出可能抛出的异常及其触发条件

---

## 代码示例风格

### 基本要求
- 使用 Python 类型注解
- 每个示例前加简短说明
- 代码保持在 10 行以内
- 忓悔释使用英文，文档注释使用中文

### 示例模板
```python
from mymodule import Client

# 创建客户端实例
client = Client(host="localhost", port=8080)

# 执行操作
result = client.query("SELECT * FROM users")
print(result)
```

---

## 语言风格

### 通用要求
- 使用简洁的技术中文
- 避免使用"我们"、"你"等人称代词
- 使用主动语态，避免被动语态
- 专业术语首次出现时提供英文原文

### 示例

**不好的写法**：
> 你可以使用这个函数来获取用户信息。我们将返回一个包含数据的字典。

**好的写法**：
> 使用此函数获取用户信息。返回包含用户数据的字典。

---

## 文档结构

### 章节标题
- 使用简洁明了的标题
- 避免过长的标题（建议不超过 15 个字）

### 内容组织
- 使用三级标题（###）组织小节内容
- 重要信息使用 **加粗** 或 `代码格式` 突出
- 列表项使用 `-` 符号

### 链接引用
- 相关内容使用相对链接
- 外部资源使用完整 URL

---

## 特殊内容

### 警告提示
使用 Markdown 引用格式：
> ⚠️ **注意**：此功能在 v2.0 版本中已废弃，请使用新接口。

### 版本说明
标注 API 变更：
- `新增于 v1.2.0`
- `已废弃 v2.0.0`
- `将在 v3.0.0 移除`